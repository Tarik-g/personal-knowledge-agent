# Local answer generation works with these defaults and needs no API key.
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen3:1.7b

# Optional until database-backed document persistence is connected.
DATABASE_URL=postgresql://USERNAME:PASSWORD@HOST/jarvis?sslmode=require
